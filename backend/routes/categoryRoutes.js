const router=require('express').Router(),c=require('../controllers/catalogController'),{protect,allow}=require('../middleware/auth');router.get('/',c.list('categories'));router.post('/',protect,allow('admin'),c.create('categories'));router.put('/:id',protect,allow('admin'),c.update('categories'));router.delete('/:id',protect,allow('admin'),c.remove('categories'));module.exports=router;
