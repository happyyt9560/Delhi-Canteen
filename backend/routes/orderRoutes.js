const router=require('express').Router(),c=require('../controllers/orderController'),{protect,allow}=require('../middleware/auth');router.use(protect);router.get('/',allow('admin'),c.list);router.get('/:id',c.detail);router.patch('/:id/status',allow('admin'),c.updateStatus);router.patch('/:id/payment-status',allow('admin'),c.updatePaymentStatus);router.post('/',allow('customer'),c.create);module.exports=router;
