const router=require('express').Router(),c=require('../controllers/deliveryController'),{protect,allow}=require('../middleware/auth');router.use(protect,allow('delivery'));router.get('/profile',c.profile);router.get('/dashboard',c.dashboard);router.get('/orders/today',c.today);router.get('/orders/today/delivered',c.todayDelivered);router.get('/orders/history',c.history);router.get('/orders/:id',c.detail);router.post('/orders/:id/reached',c.reached);router.post('/orders/:id/complete',c.complete);module.exports=router;
