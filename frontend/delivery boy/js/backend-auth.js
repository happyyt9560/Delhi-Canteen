// Backend login bridge. The API returns the delivery partner profile and JWT session.
const DELIVERY_API_URL='http://localhost:5000/api';
if(document.body.classList.contains('login-body')){const form=document.querySelector('#login-form'),phone=document.querySelector('#phone'),password=document.querySelector('#password');form.onsubmit=async event=>{event.preventDefault();const button=form.querySelector('[type="submit"]');button.disabled=true;button.textContent='Logging in...';try{const response=await fetch(`${DELIVERY_API_URL}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({identifier:phone.value.trim(),password:password.value,role:'delivery'})}),data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||'Invalid phone number or password. Please contact the administrator.');if(!data.token||!data.user)throw new Error('The server returned an invalid login response.');localStorage.setItem('deliveryBoyToken',data.token);localStorage.setItem('deliveryBoySession',JSON.stringify(data.user));localStorage.setItem('deliveryBoyLoggedIn','true');toast('Login successful! Welcome back.');setTimeout(()=>location.href='dashboard.html',650)}catch(error){toast(error.message==='Failed to fetch'?'Unable to reach the server. Please ensure the backend is running.':error.message,'error');button.disabled=false;button.innerHTML='Login <span>→</span>'}}}
