async function renderDashboard(){try{const [{data:summary},{data:profile}]=await Promise.all([deliveryApi('/delivery/dashboard'),deliveryApi('/delivery/profile')]);localStorage.setItem('deliveryBoySession',JSON.stringify(profile));DeliveryStore.profile=profile;const firstName=profile.name.split(' ')[0],reports=[['Assigned Orders',summary.today],['Delivered',summary.delivered],['Pending',summary.pending],['Cash Collected',deliveryMoney(summary.cashCollected)]];shell(`<div class="page-head"><div><h1 class="page-title">Good Afternoon, ${firstName} 👋</h1><p class="muted">Here is your delivery summary for today.</p></div></div><section class="stats"><div class="stat-card"><span class="stat-icon">▣</span><p>Today's Orders</p><strong>${summary.today}</strong></div><div class="stat-card"><span class="stat-icon">◷</span><p>Pending Orders</p><strong>${summary.pending}</strong></div><div class="stat-card"><span class="stat-icon">✓</span><p>Delivered Orders</p><strong>${summary.delivered}</strong></div><div class="stat-card"><span class="stat-icon">₹</span><p>Cash Collection Today</p><strong>${deliveryMoney(summary.cashCollected)}</strong></div></section><section class="dashboard-grid"><article class="panel"><h2>Reports & Summary</h2>${reports.map(row=>`<div class="report-row"><span>${row[0]}</span><b>${row[1]}</b></div>`).join('')}</article><article class="panel"><h2>Today's progress</h2><p class="muted">${summary.delivered} of ${summary.today} deliveries completed</p><div class="bar"><i style="width:${summary.today?summary.delivered/summary.today*100:0}%"></i></div><p class="muted">Keep going — every completed delivery counts.</p></article></section>`)}catch(error){shell(`<div class="panel empty"><div>⚠</div><b>Could not load your dashboard.</b><p>${error.message}</p></div>`)}}
renderDashboard();
